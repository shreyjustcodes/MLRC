# ML Research Club — Website

Next.js (App Router) + Tailwind + TypeScript + embedded Sanity Studio.

## Quick start

```bash
npm install
npm run dev
```

- Site: <http://localhost:3000>
- CMS: <http://localhost:3000/studio>

## Sanity

Full setup (CORS, first documents, deploy): [docs/SANITY_SETUP.md](docs/SANITY_SETUP.md).

Project ID: `qfalq9hj` · Dataset: `production` (see `.env.example` / `.env.local`).
